import React, { FC } from 'react';
import { BarberShop } from '../../types/barberShop';
import { View } from 'react-native';

interface Props {
    topBarberShops: BarberShop[]
}


const TopBarberShops: FC<Props> = (props) => {
    return (
        <View>

        </View>
    )
}

export default TopBarberShops;